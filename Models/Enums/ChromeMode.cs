namespace Stratus.Models.Enums
{
    public enum ChromeMode
    {
        Full, //Titlebar and tabs
        Tabs, //Just tabs and buttons
        Autohide,//Tab mode auto hides/appears when mouse is near the top of the window
        Minimal,//Nothing but buttons
    }
}
